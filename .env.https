# Environment variables for HTTPS mode
# Set USE_HTTPS=true to enable HTTPS in Azurite and configure HTTPS endpoints in Dapr components
USE_HTTPS=true

# Web application HTTPS configuration
# Uncomment the following line to run the web app with HTTPS
# ASPNETCORE_URLS=https://*:8443;http://*:8080

# Additional HTTPS configuration
AZURITE_BLOB_ENDPOINT=https://127.0.0.1:10000
AZURITE_QUEUE_ENDPOINT=https://127.0.0.1:10001